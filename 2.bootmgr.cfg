    echo Searching... /efi/boot/bootmgr.efi
search --file --no-floppy --set=root /bootmgr.efi

    echo Loading... kernel
chainloader (${root})/bootmgr.efi

    echo Booting... bootmgr.efi
boot